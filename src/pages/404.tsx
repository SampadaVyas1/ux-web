import NotFoundComp from "@/views/page-not-found";
import { parallaxData } from "@/views/page-not-found/parallax/parallax.data";

const NotFound = () => <NotFoundComp />;

NotFound.parallaxData = parallaxData;

export default NotFound;
